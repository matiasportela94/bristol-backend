# Production profile
spring:
  datasource:
    hikari:
      maximum-pool-size: 5
      minimum-idle: 2
  jpa:
    show-sql: false

logging:
  level:
    root: WARN
    com.bristol: INFO
    org.springframework.web: WARN
    org.hibernate.SQL: WARN
