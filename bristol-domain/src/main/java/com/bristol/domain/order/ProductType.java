package com.bristol.domain.order;

/**
 * Product type enumeration for order items.
 * Matches the product_type ENUM in the database schema.
 */
public enum ProductType {
    BEER,
    MERCH,
    SPECIAL
}
